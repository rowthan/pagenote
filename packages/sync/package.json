{
  "name": "@pagenote/sync",
  "version": "0.1.19-dev.0",
  "license": "MIT",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "typings": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "tsc -p tsconfig.json --watch",
    "start": "tsc -p tsconfig.json --watch",
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "tsdx test",
    "lint": "tsdx lint",
    "prepare": "rm -rf dist && tsc -p tsconfig.json",
    "size": "size-limit",
    "analyze": "size-limit --why"
  },
  "husky": {
    "hooks": {
      "pre-commit": "tsdx lint"
    }
  },
  "prettier": {
    "printWidth": 80,
    "semi": true,
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "author": "rowthan",
  "size-limit": [
    {
      "path": "dist/sync.cjs.production.min.js",
      "limit": "10 KB"
    },
    {
      "path": "dist/sync.esm.js",
      "limit": "10 KB"
    }
  ],
  "devDependencies": {
    "@size-limit/preset-small-lib": "^11.0.1",
    "husky": "^8.0.3",
    "size-limit": "^11.0.1",
    "tsdx": "^0.14.1",
    "tslib": "^2.6.2",
    "typescript": "^5.3.3"
  }
}
