{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "version": "independent",
  "npmClient": "yarn",
  "packages": [
    "packages/*"
  ],
  "ignoreChanges": [
    "**/__fixtures__/**",
    "**/__tests__/**",
    "**/*.md",
    "yarn.lock",
    "**/package.json",
    "**/*.yml"
  ],
  "command": {
    "publish": {
      "allowBranch": ["main","dev","feat*"]
    }
  }
}
